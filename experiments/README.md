Experiments
===

`notebooks`: Jupyter notebooks with experiments

`pmlib`: a Python package containing functions and classes that are used withing the Jupyter notebooks

`pyproject.toml`, `poetry.lock`: files for Poetry package manager to install the same Python dependencies as used when running the experiments

`requirements.txt`: an alternative to the above file for Poetry - a list of dependencies to be installed by pip, a standard Python package-management system
